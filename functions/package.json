{
  "name": "niqdah-functions",
  "description": "Secure Firebase Cloud Functions backend for Niqdah",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.9.0",
    "firebase-functions": "^7.2.5",
    "openai": "^6.37.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.40",
    "typescript": "^6.0.3"
  }
}
